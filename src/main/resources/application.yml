
server:
  error:
    include-message: always
    include-binding-errors: always

spring:
  application:
    name: Login system with email verification

  datasource:
    url: jdbc:mysql://localhost:3306/users_login
    username: root
    password: admin@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

logging:
  level:
    org.springframework.security: Debug

